#!/bin/bash

arch=$(uname -m)
if [ "$OS" = 'darwin' ]; then
  os='osx'
fi

if command -v ghc &>/dev/null; then
  ghc=$(ghc --version | awk '{print $(NF)}')
  echo "${arch}-${OS}-ghc-${ghc}"
else
  onoe 'GHC is not installed'
  exit 1
fi
