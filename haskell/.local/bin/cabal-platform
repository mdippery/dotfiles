#!/bin/bash

arch=$(uname -m)
if [ "$OS" = darwin ]; then
  os=osx
fi

if command -v ghc &>/dev/null; then
  ghc=$(ghc --version | awk '{print $(NF)}')
  echo "${arch}-${OS}-ghc-${ghc}"
else
  echo 'GHC is not installed' >&2
  exit 1
fi
