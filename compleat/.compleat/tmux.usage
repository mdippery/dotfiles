tmux (a | attach) [-t <sessions>];
tmux (ls | list-sessions);
tmux kill-server;
tmux kill-session [-t <sessions>];
tmux (new | new-session) [-s <SESSION>];

sessions = ! tmux ls 2>/dev/null | cut -d ':' -f 1 ;
