#!/bin/bash

function fd_ext {
  fd -e $1 . $2
}

function find_ext {
  find "$2" -name '*.'$1
}

ext=$1
path=${2:-.}
if command -v fd &>/dev/null; then
  fd_ext $ext "$path"
else
  find_ext $ext "$path"
fi
