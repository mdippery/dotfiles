#!/bin/bash

if [ "$(type -t $1)" = file ]; then
  if command -v $1 &>/dev/null; then
    readlink -f $(which $1);
  else
    which $1
  fi
else
  if command -v $1 &>/dev/null; then
    type $1
  else
    onoe "$0: $1: command not found"
    exit 1
  fi
fi
